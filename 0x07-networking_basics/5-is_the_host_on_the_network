#!/usr/bin/env bash

# Check if exactly one argument is provided
if [ $# -eq 1 ]; then
  # Ping the IP address 5 times with quotes around $1
  ping -c 5 "$1"
else
  # Display usage message
  echo "Usage: ./ping_script.sh {IP_ADDRESS}"
fi
